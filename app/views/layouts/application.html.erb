<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>Dogfeeds</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag '/favicon.ico' %>
    <%= stylesheet_pack_tag 'application' %>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="RSSフィードをまとめたRSSフィードを作ることができるWebサービスです">
    <%= render 'layouts/ogp_tags' %>
    <%= render 'layouts/google_analytics_tags' %>
  </head>

  <body>
    <header id="vue-header"><navigation-bar /></header>
    <div id="wrapper">
      <%= yield %>
    </div>
    <%= render 'layouts/footer' %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
